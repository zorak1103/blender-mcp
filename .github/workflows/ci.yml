name: CI

on:
  push:
  pull_request:
  workflow_call:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: uv pip install -e ".[test]" ruff mypy --system

      - name: Lint (ruff)
        run: ruff check .

      - name: Type check (mypy)
        run: mypy blender_addon/ launcher.py

      - name: Unit tests
        run: pytest tests/unit/ -v --cov=blender_addon --cov=launcher --cov-fail-under=80
